# Phase 1 cost report

Status: **awaiting live evidence**

The app records transcription audio, primary normalization, and shadow
normalization separately and stops at the configured hard cap.

For the 30–60 minute validation session, record:

- wall-clock duration and submitted audio per channel;
- in-app cost estimate by stage;
- OpenAI dashboard/project usage;
- variance between estimate and provider usage;
- VAD-on versus VAD-off submitted audio;
- extrapolated cost per 60 minutes and per 4.33-session month.

Do not use the Realtime Translate four-stream estimate as measured spend.
